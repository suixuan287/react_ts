import * as React from 'react'
const { useState, useEffect } = React

interface Props {
    name: string,
    title: string
}

const Home = () => {

    return (
        <h1>
            home
        </h1>
    )
}

export default Home